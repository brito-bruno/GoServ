/**
 * Funções reutilizáveis do client — importe daqui em vez de duplicar nas páginas.
 */
export { formatMoney, formatPrice } from './formatMoney'
export { formatClock, formatCountdown } from './dates'
